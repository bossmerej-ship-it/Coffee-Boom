# AI Assistance & Usage Log

**Developer:** Timur
**AI Model Used:** Claude (Anthropic)
**Academic Honesty Statement:** Claude was employed as a technical reference and workflow assistant for OOP design guidance, GUI (customtkinter) implementation, PostgreSQL integration, and report-drafting support on the Car Rental Sale System project. All code output was manually reviewed, adapted to project requirements, and tested before inclusion. No content was copied without understanding or verification.

## Log of Interactions

| Date | Intent & User Prompt | Claude Output & Recommended Solution | Critical Evaluation & Student Implementation |
|---|---|---|---|
| 2026-09-05 | Designing the core class hierarchy (Car, Customer, Rental) using proper OOP principles for the sale system. | Claude proposed a base `Vehicle` class with `Car` as a subclass, encapsulated attributes with getters/setters, and a separate `Rental` class to manage transaction state rather than embedding logic in `Car`. | Adopted the separation of `Rental` from `Car` to keep responsibilities clear; renamed attributes to match project's naming conventions and added validation in setters. |
| 2026-09-06 | Structuring the customtkinter GUI layout for the main dashboard (car list, filters, action buttons). | Claude suggested a grid-based layout using `CTkFrame` containers, with a scrollable frame for the car listing and a separate sidebar frame for filters/actions. | Implemented the two-frame layout in `main_window.py`; adjusted padding and widget sizing manually to match the report's screenshots. |
| 2026-09-07 | Connecting the application to PostgreSQL and handling connection errors gracefully. | Claude recommended using `psycopg2` with a context-managed connection, wrapping queries in try/except blocks, and centralizing the connection logic in a single `db_handler.py` module. | Refactored scattered database calls into the suggested `db_handler.py`; tested against local PostgreSQL instance and confirmed error messages display correctly in the GUI. |
| 2026-09-08 | Resolving a `psycopg2.OperationalError` caused by mismatched database credentials after switching machines. | Claude identified the likely cause (hardcoded local credentials) and suggested moving connection parameters to a config file/environment variables. | Moved credentials to a local `config.py` (excluded from Git via `.gitignore`); verified connection worked across both development machines. |
| 2026-09-09 | Implementing input validation for the rental form (dates, customer ID, car availability check). | Claude suggested validating date ranges before querying availability, and disabling the "Confirm" button until all required fields pass validation. | Implemented the validation order as suggested in `rental_form.py`; added inline error labels instead of pop-up dialogs to match the GUI style used elsewhere in the app. |
| 2026-09-10 | Structuring the academic report to stay in sync with the evolving codebase (class diagrams, module descriptions). | Claude recommended organizing the report by module (data layer, GUI layer, business logic) rather than chronologically, and cross-referencing class diagrams to actual file names. | Reorganized the report sections accordingly; regenerated the class diagram manually to reflect the current `Rental`/`Car` structure before submission. |
| 2026-09-11 | Preparing likely defense questions about design choices (why separate `Rental` from `Car`, why PostgreSQL over SQLite). | Claude outlined the trade-offs (concurrent access, scalability, separation of concerns) to help anticipate committee questions. | Used the reasoning to prepare verbal answers for the defense; verified the explanation matched the actual implementation before finalizing talking points. |
